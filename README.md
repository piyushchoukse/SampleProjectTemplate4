# SampleProjectTemplate4
A iOS project structure template create a basic project structure with folders as per user input, implement force full crash for unwrapping a optional, Read the crash log and write to crashlog.txt, create jira bug by calling MCPConnectAgent
